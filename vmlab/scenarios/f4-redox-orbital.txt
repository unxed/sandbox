# f4 (github.com/unxed/f4, GOOS=redox build from .github/workflows/f4-redox.yml) in the real
# Orbital/COSMIC terminal of the Redox desktop image. Recorded from an interactive session.
#
# Prerequisites (start the session with the warm snapshot, then fetch the binary through the agent):
#   python3 vmlab/ctl.py start --guest redox-uefi --minutes 30 --loadvm desk-agent
#   python3 vmlab/ctl.py sh 'curl -sL -o /tmp/f4n https://github.com/unxed/sandbox/releases/download/f4-redox-latest/f4-redox-amd64 &> /tmp/curl.log; chmod +x /tmp/f4n; /tmp/f4n --version'
# The snapshot's terminal window runs the guest agent, so f4 goes into a NEW tab ("+" at 1008,51).
# Coordinates are for 1280x800.
click 1008 51
wait 4
shot newtab
typeln /tmp/f4n --tty --attached
wait 20
shot f4-start
key f1
wait 4
shot help-f1
key esc
wait 2
key down
key down
key down
wait 2
shot cursor-down
key f9
wait 3
shot menu-f9
key esc
wait 1
key f3
wait 3
shot view-f3
key esc
wait 2
